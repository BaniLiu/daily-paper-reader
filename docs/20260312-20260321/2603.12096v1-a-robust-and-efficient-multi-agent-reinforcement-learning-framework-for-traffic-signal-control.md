---
title: A Robust and Efficient Multi-Agent Reinforcement Learning Framework for Traffic Signal Control
title_zh: 一种用于交通信号控制的鲁棒且高效的多智能体强化学习框架
authors: "Sheng-You Huang, Hsiao-Chuan Chang, Yen-Chi Chen, Ting-Han Wei, I-Hau Yeh, Sheng-Yao Kuan, Chien-Yao Wang, Hsuan-Han Lee, I-Chen Wu"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12096v1"
tags: ["query:vsl-rl"]
score: 8.0
evidence: 用于交通信号控制的多智能体强化学习
tldr: 本研究针对交通信号控制中强化学习模型泛化能力弱和动作空间不稳定的挑战，提出一种鲁棒的多智能体强化学习框架。该框架通过转向比例随机化增强鲁棒性，采用指数级相位时长调整平衡响应速度与精度，并利用基于邻居观测的MAPPO算法实现高效协作。在Vissim模拟器中的实验证明，该方法显著降低了车辆等待时间，在动态交通流中表现出优异的泛化性和控制稳定性。
selection_source: fresh_fetch
motivation: 现有交通信号控制强化学习方法在动态流量下泛化性差，且动作空间设计往往不符合实际驾驶预期。
method: 提出集成转向比例随机化、指数级相位时长调整动作空间及基于邻居观测的MAPPO算法的MARL框架。
result: "在Vissim模拟器中，该框架将平均等待时间降低了10%以上，并在未知场景中展现出卓越的泛化能力。"
conclusion: 该框架为实际部署自适应交通信号控制提供了一种兼顾鲁棒性、稳定性和可扩展性的有效解决方案。
---

## 摘要
交通信号控制（TSC）中的强化学习（RL）在实际部署中面临重大障碍，原因是对动态交通流变化的泛化能力有限。现有方法通常过拟合静态模式，且使用的动作空间与驾驶员预期不符。本文提出了一种在 Vissim 交通模拟器中验证的鲁棒多智能体强化学习（MARL）框架。该框架集成了三种机制：(1) 转向比随机化（Turning Ratio Randomization），这是一种通过让智能体接触动态转向概率来增强对未知场景鲁棒性的训练策略；(2) 面向稳定性的指数相位时长调整（Exponential Phase Duration Adjustment）动作空间，通过周期性的指数相位调整来平衡响应速度和精度；(3) 基于邻域的观测方案，利用带有集中式训练分布式执行（CTDE）的 MAPPO 算法。通过利用集中式更新，该方法在保持可扩展本地通信的同时，接近了全局观测的效果。实验结果表明，我们的框架优于标准的强化学习基准，将平均等待时间减少了 10% 以上。所提出的模型在未知交通场景中表现出卓越的泛化能力，并保持了较高的控制稳定性，为自适应信号控制提供了一种实用的解决方案。

## Abstract
Reinforcement Learning (RL) in Traffic Signal Control (TSC) faces significant hurdles in real-world deployment due to limited generalization to dynamic traffic flow variations. Existing approaches often overfit static patterns and use action spaces incompatible with driver expectations. This paper proposes a robust Multi-Agent Reinforcement Learning (MARL) framework validated in the Vissim traffic simulator. The framework integrates three mechanisms: (1) Turning Ratio Randomization, a training strategy that exposes agents to dynamic turning probabilities to enhance robustness against unseen scenarios; (2) a stability-oriented Exponential Phase Duration Adjustment action space, which balances responsiveness and precision through cyclical, exponential phase adjustments; and (3) a Neighbor-Based Observation scheme utilizing the MAPPO algorithm with Centralized Training with Decentralized Execution (CTDE). By leveraging centralized updates, this approach approximates the efficacy of global observations while maintaining scalable local communication. Experimental results demonstrate that our framework outperforms standard RL baselines, reducing average waiting time by over 10%. The proposed model exhibits superior generalization in unseen traffic scenarios and maintains high control stability, offering a practical solution for adaptive signal control.

---

## 论文详细总结（自动生成）

这篇论文提出了一种用于交通信号控制（TSC）的鲁棒且高效的多智能体强化学习（MARL）框架，旨在解决强化学习在实际交通部署中面临的泛化性差、动作空间不稳定及可扩展性不足等核心痛点。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管深度强化学习在模拟环境中表现优异，但在现实部署中存在三大障碍：
    1.  **泛化性差**：智能体容易过拟合静态的训练交通流模式，难以应对现实中非平稳、随机的交通变化。
    2.  **动作空间不切实际**：许多研究采用非周期性或剧烈波动的信号切换，不符合驾驶员预期且存在安全隐患。
    3.  **可扩展性难题**：全局观测在大型路网中维度爆炸，而局部观测又缺乏协作能力。
*   **核心目标**：开发一个既能保证控制稳定性，又能灵活应对动态流量，且具备良好扩展性的鲁棒 MARL 框架。

### 2. 论文提出的方法论
该框架集成了三项关键技术创新：
*   **转向比例随机化（Turning Ratio Randomization）**：
    *   在训练阶段，对每个路口的转向概率进行随机扰动（通过统一分布缩放并重新归一化）。
    *   **目的**：防止智能体“背诵”固定的配时方案，迫使模型学习根据实时车流状态进行反应，从而增强对未知场景的鲁棒性。
*   **指数级相位时长调整（Exponential Phase Duration Adjustment）**：
    *   采用周期性控制（Cyclic Control），动作空间设计为指数级步长：$\Delta t \in \{ 0, \pm\lambda^0, \pm\lambda^1, \pm\lambda^2, \pm\lambda^3\}$（如 $\lambda=2$ 时，步长为 $0, \pm1, \pm2, \pm4, \pm8$ 秒）。
    *   **目的**：实现“由粗到精”的控制。大步长用于快速缓解突发拥堵，小步长用于稳态时的精细微调，平衡了响应速度与系统稳定性。
*   **基于邻居观测的 CTDE 架构**：
    *   利用 **MAPPO** 算法，采用“集中式训练、分布式执行”（CTDE）模式。
    *   **关键细节**：智能体在执行时仅观测自身及直接相邻路口的状态（Neighbor-level Observation），但在训练时通过全局 Critic 评估动作影响。
    *   **目的**：在保持计算可扩展性的同时，通过邻里协作接近全局最优效果。

### 3. 实验设计
*   **模拟环境**：使用高保真微观交通模拟软件 **PTV Vissim**（而非简化的 SUMO），以缩小“从模拟到现实”的差距。
*   **场景设置**：以台湾桃园市中正东路的数字孪生模型为背景，包含 5 个连续的信号交叉口。
*   **数据集**：基于 24 小时真实探测器数据，提取“高峰时段”（高负载）和“非高峰时段”（中等负载）进行交叉验证。
*   **Benchmark 与对比方法**：
    *   **传统方法**：固定配时（FixTime）、最大压力启发式算法（MaxPressure）。
    *   **强化学习基准**：标准 RL（分为局部、邻居、全局观测三种模式）。
    *   **消融对比**：IPPO（非集中式训练）vs MAPPO；线性调整 vs 指数调整。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或具体的训练时长。考虑到 Vissim 模拟器的运行开销通常高于轻量级模拟器，其训练过程可能对 CPU 和内存有较高要求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了高峰与非高峰场景的交叉泛化实验。
    *   针对 CTDE 架构、动作空间设计（线性 vs 指数）、观测范围（局部 vs 邻居 vs 全局）进行了多组消融实验。
*   **充分性评价**：实验设计较为充分且客观。通过在高峰期训练、非高峰期测试的策略，严谨地验证了模型的泛化能力。对比了多种主流的 TSC 控制逻辑，证明了所提组件的必要性。

### 6. 主要结论与发现
*   **性能提升**：该框架在未知场景下将平均等待时间（AWT）降低了 **10% 以上**。
*   **鲁棒性验证**：未经过随机化训练的标准 RL 在非高峰场景（未知分布）中表现甚至不如传统启发式方法，而经过转向比随机化的模型表现稳定。
*   **动作空间优势**：指数级调整在所有指标上均优于线性调整，证明了其在处理突发流量和维持稳态方面的双重优势。
*   **协作效率**：基于邻居观测的 MAPPO 效果接近全局观测，且远优于完全局部的观测模式。

### 7. 优点
*   **高保真度**：使用 Vissim 进行验证，比 SUMO 等工具更具行业说服力。
*   **实用性强**：指数级步长和周期性相位设计非常符合交通工程的实际约束和驾驶员心理。
*   **泛化策略简单有效**：转向比随机化是一种低成本但高效的正则化手段，解决了 TSC 领域长期的过拟合问题。

### 8. 不足与局限
*   **路网规模有限**：实验仅在 5 个路口的干线（Arterial）上进行，尚未在更大规模的网格（Grid）路网中验证其极端扩展性。
*   **交通构成单一**：仅考虑了四轮汽车，未涉及摩托车、行人或公共交通优先等复杂多模态交通流。
*   **实时通信假设**：模型假设邻里间存在可靠的实时通信，在现实中可能面临网络延迟或丢包的挑战。

（完）
